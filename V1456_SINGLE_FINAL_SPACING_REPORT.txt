AI Running Coach v14.5.6 · build 40506

FIXED: contradictory purchase-spacing text versus displayed first-use dates and graph.

Root cause
The purchase-spacing optimiser was still called twice:
1. before Race Day planning;
2. after final Race Day / weekly rebalancing.

The early pass could record a successful 84-day spacing result such as "Purchase brought forward to 2026-12-22".
Later allocation could move those early sessions back to February. The old spacingReason survived, so the card
could simultaneously show February as first use and December in its explanation.

Correction
- Removed the pre-Race purchase-spacing pass entirely.
- Exactly one purchase-spacing pass remains in freshShoeLifecyclePlan().
- It runs only after every Race Day and weekly assignment mutation has finished.
- It clears all prior spacing metadata before calculation.
- Purchase date / first-use date are canonicalised from the final physical-pair assignment ledger.
- Graph BUY markers are rebuilt after those canonical dates.
- Stale earlier-date spacing prose is removed before the final spacing explanation is appended.

Consistency invariant
The explanatory spacing date, "Need by / first use" date, physical-pair availability date and graph BUY marker
must all originate from the same final assignment ledger.
